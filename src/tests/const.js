const DIC_DIR = "dictionaries";

// determins is the libray is loaded with promises
// if false the the test harnses will assume orignal old lib is being run and load with callbacks
const TEST_ORIG = true; 